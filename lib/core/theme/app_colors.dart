import 'package:flutter/material.dart';

class AppColors {
  // Colors inspired by the new earthy background image
  static const Color backgroundGlow = Color(0xFFFFD38A); // Warm glow at top
  static const Color backgroundBase = Color(0xFF6F432B); // Main texture tone
  static const Color backgroundDark = Color(0xFF3D2319); // Darkest corners

  // Surfaces & Cards
  static const Color surface = Color(0xFF4A2A1A); // Deeper brown for cards on top
  static const Color surfaceLight = Color(0xFF5D3823);

  // Bottom Navigation
  static const Color bottomNavBackground = Color(0xFF3D2314);

  // Floating Action Button
  static const Color fabBackground = Color(0xFF29C3FF);
  static const Color fabRing = Color(0xFFC08945); // Emas

  // Text Colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFD3C5BC);

  // Borders & Chips
  static const Color border = Color(0xFF6B4832);
  static const Color chipBackground = Color(0xFF3A1F11);
}